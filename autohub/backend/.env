# Database (AlwaysData)
DB_CONNECTION=mysql
DB_HOST=mysql-automotive-hub.alwaysdata.net
DB_PORT=3306
DB_NAME=automotive-hub_db
DB_USER=435371
DB_PASSWORD=rtHyS9bn7Xff3sV

# Aliases used in some files
MYSQL_HOST=${DB_HOST}
MYSQL_PORT=${DB_PORT}
MYSQL_DATABASE=${DB_NAME}
MYSQL_USER=${DB_USER}
MYSQL_PASSWORD=${DB_PASSWORD}

# App / runtime
JWT_SECRET=replace_with_a_strong_secret
NODE_ENV=production
PORT=5000
